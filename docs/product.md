# 产品文档

## 定位

面向运营/电商用户：把网页上的表格（尤其是 ERP 后台列表）快速导出成 Excel / CSV / JSON / Markdown / HTML，无需手动复制粘贴或截图。典型场景：点三咪 TikTok 促销活动编辑页的商品列表导出。

## 核心用户故事

1. 我打开一个页面，想把某个表格变成 Excel → 点击扩展图标，点击表格，导出
2. 页面有多个表格，我想一次性导出 → 多选表格，每个变成一个 Sheet
3. 表格有合并的表头 → Excel 里保持合并效果
4. 表格是虚拟滚动的长列表 → 扩展自动滚动采集全部行
5. 表格里有输入框/下拉/开关等控件（一口价等编辑值） → 导出的是当前显示/输入的值（规则见 docs/controls.md）
6. 一列里既有输入框又有文字（如「2249 PHP」），我想拆成两列 → 「列设置」面板配置，控件值与文本各成一列
7. 一格里放了多个输入框（如秒杀价 + 秒杀库存），我想各自成一列 → 面板「控件值拆分」，每个控件单独一列（不再用顿号合并）
8. 「标题/产品ID」这类一格两行的列，我想标题和 ID 各成一列 → 面板「按换行拆分」，行内空格不拆
9. 组件库（Element Plus 等）把表头和数据行渲染成两个表格 → 自动识别为一个表格整体，点一次即选中，导出单个 Sheet
10. 我想自己命名文件 → 工具栏可改文件名
11. 表格有些列我不需要（如操作/日志列），不想导出后再删 → 「列设置」面板取消勾选「导出」，拆分产生的新列同样可单独筛掉
12. 表格里有商品图片 → 单元格导出图片的链接（绝对地址），在 Excel 里可直接打开
13. 我配置过的拆分规则与列筛选，下次再导出同页面时自动生效 → 保存后按「页面 + 表头指纹」记住（本地存储，不上传）；表头变了则回落默认，重新配置即可
14. 价格/数量列导出后在 Excel 里想直接求和，订单号/产品ID 列想保持文本 → 「列设置」面板把列格式设为「数字」（默认文本）：数字列数值化写入（含千分位逗号先剥离，无法解析保持原文本），拆分产生的新列同样生效
15. 导出的 Excel 列宽乱七八糟，窄列挤成一条线、长标题列又太宽 → 列宽随内容自适应（中文按双宽估算），钳制在 6~50 字符：短列不挤、超长列（如商品标题）封顶换行，打开即可读
16. 我想要的不只是 Excel：数据要给程序处理（csv/json）、要贴进文档/笔记（md）、要直接浏览器查看（html）→ 工具栏格式下拉切换，列设置（拆分/筛选/列格式）对全格式一致生效

## 交互规范

### 入口与退出

| 操作 | 行为 |
|---|---|
| 点击扩展图标 | 进入选择模式（重复注入由守卫接管：已激活则退出） |
| 再次点击图标 / Esc / 取消按钮 | 退出选择模式，清理全部 UI 与监听 |
| Enter | 导出（焦点在按钮上时走按钮默认行为） |

### 选择模式

- 悬浮任意 `<table>`：蓝色高亮框跟随；组件库分体表格（表头/表体两个 table）高亮、点选均以整体识别
- 点击表格：选中（绿色覆盖层 + 左上角序号徽标）；再点取消
- 选择模式下页面点击被拦截，不触发跳转/操作（工具栏自身除外）
- 工具栏（底部居中）：提示文案（含状态反馈）、已选计数、文件名输入框、导出格式下拉（xlsx / csv / json / md / html，默认 xlsx，切换时导出按钮文案同步）、列设置按钮（未选时禁用）、导出按钮（未选时禁用）、取消按钮

### 列设置面板（列筛选 + 拆分配置 + 列格式）

- 「列设置」按钮：仅已选 ≥ 1 个表格时可用（与导出按钮同一启用条件）
- 面板流程：选表格（Sheet 序号下拉）→ 列清单（表头名，控件列/多行列带徽标）→ 配置导出勾选 / 拆分模式与参数 / 列格式 → 前 3 行预览实时刷新（划线列为不导出）→ 保存
- 列筛选：每列最前有「导出」勾选（默认全选）；勾选拆分时下方子行展示各新列的独立导出勾选；顶部工具条显示「导出列 已选/全部」并提供「全选 / 全不选」快捷按钮
- 列格式：每列行末「格式」下拉（文本 = 默认 / 数字）；数字列导出时数据行数值化写入 Excel（'1,234' → 1234，表头行不动，无法解析的值保持原文本）；作用于该列及其拆分新列（含合并单元格的表格也可设置，拆分与列筛选仍禁用）
- 三种拆分模式：
  - 控件值拆分：该列按控件拆为「原名_控件」（输入框实时值）+「原名_文本」（页面文字），如「2249 PHP」→「2249」与「PHP」；同格多个控件各自成一列（「原名_控件1 / 原名_控件2…」+ 末尾文本列，控件数不足的行补空）
  - 按换行拆分：格内多行文本（如「标题/产品ID」双行格）按行拆成多列，行内空格不拆
  - 分隔符拆分：按分隔符把文本拆为多段，新列名「原名1 / 原名2…」；段数上限可选（超限并入末段）
- 一列一拆分规则；原列一律保留（新列追加其后，拆错可直接删列或取消该列导出）
- 智能预填：多行文本列默认勾选拆分并预设「按换行拆分」；含控件的列预设「控件值拆分」但不勾选（由用户确认）；其余纯文本列探测分隔符预填（默认不勾选），均可改
- 空格分隔符以「␣」标记显示；面板内 Esc 关面板、Enter 保存；面板打开时主工具栏导出/取消禁用
- 规则、列筛选与列格式保存后持久化（本地 chrome.storage，按页面 + 表头指纹定位）：取消选择/退出不清空，重选同表自动恢复（提示「已恢复上次的列设置」）；表头变更后指纹不匹配则回落智能预填默认；全部拆分取消勾选 + 全列导出 + 全列文本格式再保存 = 重置（删除记录）；含合并单元格的表格禁用拆分与列筛选（列格式仍可设置）

### 虚拟滚动表格

点击后自动执行：回顶 → 逐步滚动 → 逐窗口提取行（自动衔接去重）→ 还原滚动位置。期间：

- 工具栏实时显示「已采集 N 行」；按钮禁用、表格点击无响应（防误操作）
- 完成后显示「采集完成，共 N 行（含表头）」
- 虚拟表格须先采集完成才能在列设置面板中取样

### 导出结果（全格式公共）

- 单元格文本：视觉上分离的多段文本（换行/连续空格）以单个空格连接，如 `4722 PHP`、`标题 产品ID`；本来连在一起的文本不加空格
- 图片单元格：导出图片链接（src 解析后的绝对地址，srcset 场景取 currentSrc）；无链接图片、video/svg/iframe 导出为空
- 文件名：默认 `页面标题_yyyyMMdd-HHmmss`，可编辑；非法字符替换为 `_`；按所选格式自动补 `.xlsx` / `.csv` / `.json` / `.md` / `.html`
- 下载：经扩展后台 API（不受页面 CSP 限制），失败自动回退页面内下载
- 列设置（拆分/筛选/列格式）对全部格式一致生效

### xlsx 专属

- 多表格 → 多 Sheet，Sheet 名：caption > aria-label > id > 表格N（31 字符截断，自动去重）
- 合并单元格保留（Excel 原生 merge）
- 单元格格式：默认全部按文本写入（订单号/产品ID 前导零与长数字不丢不变形）；列格式设为「数字」的列数据行数值化写入（可在 Excel 直接求和）
- 列宽：随内容自适应——逐列取单元格视觉宽度最大值（中文/全角按 2 字符宽估算，内嵌换行取最长行），钳制在 6~50 字符；空列取下限，超长列（如商品标题）封顶换行

### csv / json / md / html 专属

- **csv**：RFC 4180 转义（含逗号/引号/换行的值双引号包裹）、UTF-8 BOM + CRLF 行尾，Excel 直接打开不乱码；多表格拆为多个文件（文件名带表名后缀，单表不带）
- **json**：单表 = 行对象数组（`[{ "列名": 值, … }, …]`），多表 = 表名键嵌套；列名取最贴近数据的表头行，空列名补「列N」、重名列加序号；数字列输出为数值
- **md**：GitHub 风格 Markdown 表格（`| 列 | … |` + `---` 分隔行），多表二级标题分区；竖线转义、单元格内换行转 `<br>`；无表头表格自动生成「列1/列2…」表头行
- **html**：完整 HTML 文档（UTF-8 声明 + 极简表格样式），表头行入 thead，浏览器直接打开即可读；多表串接
- 合并单元格不还原（平面数据）；数字列数值化后以文本形式输出

## 状态反馈文案

| 状态 | 文案 |
|---|---|
| 默认 | 点击选择表格（可多选） |
| 采集中 | 虚拟表格采集滚动中… 已采集 N 行 |
| 采集完成 | 采集完成，共 N 行（含表头），2.5 秒后恢复默认 |
| 保存 | 列设置已保存并记住，导出时生效（2.5 秒后恢复默认） |
| 恢复 | 已恢复上次的列设置（选中命中持久化记录时，2.5 秒后恢复默认） |
| 导出成功 | 已开始下载…（0.6 秒后退出选择模式） |
| 生成失败 | 导出失败：<原因>（红色） |
| 采集失败 | 采集失败：<原因>（红色） |

## 版本历史

| 版本 | 内容 |
|---|---|
| 1.0.0 | 初始版本：多选表格、合并单元格、多 Sheet、自定义文件名；下载走 chrome.downloads（修复页面 CSP 拦截） |
| 1.1 | 虚拟滚动自动采集全量行；导出 input/select 实时值；兼容无 tr 表头；单元格文本归一化 |
| 1.1.1 | 采集算法通用化：「相邻窗口重叠合并」替代全局内容去重，保留合法重复行；虚拟表格识别放宽且误报无损；渲染慢自动补等重试 |
| 1.2 | 控件值全覆盖：三层判定（原生表单 → ARIA 角色 → 组件类名）；select「显示文本(value)」、开关统一「是/否」；详见 docs/controls.md |
| 1.3 | 列拆分：控件值 / 换行 / 分隔符三模式 + 智能预填 + 前 3 行预览；原列保留；规则存内存；详见 docs/archive/column-split-plan.md |
| 1.4 | 分体表格合并：组件库（Element Plus el-table 等）表头/表体两个 table 自动识别为一个整体，悬浮高亮、点选、导出单 Sheet；表头滚动条 gutter 占位列不导出 |
| 1.5 | 导出列筛选：「列设置」面板（原拆分列面板升级）支持逐列勾选是否导出，拆分产生的各新列可独立筛选；默认全选（未配置 = 全列导出，零回归）；全选/全不选快捷操作与至少保留一列校验 |
| 1.5.1 | 控件值拆分细化：同格多个控件（如店小秘秒杀价格/库存双输入格）不再顿号合并为一列，而是每个控件各成一列（「原名_控件1..N」+ 末尾「原名_文本」），控件数不足的行补空 |
| 1.6 | 图片链接导出：单元格内 img 以其链接（src 绝对地址，srcset 取 currentSrc）作为文本参与导出、预览与列拆分；无链接图片、video/svg/iframe 仍导出为空 |
| 1.7 | 拆分规则与列筛选持久化：保存后按「页面 + 表头指纹」存本地 chrome.storage（新增 storage 权限），重选同表自动恢复（「已恢复上次的列设置」）；表头变更回落默认；保存空配置 = 重置；设计详见 docs/persist-plan.md |
| 1.8 | 性能优化（零行为变更）：单元格取值改批量两阶段离屏读取（大表格导出回流次数从每格 2-3 次降为整表常数次）；虚拟采集分体组复用 + 增量行签名；列设置面板段数计算记忆化（万行快照下勾选/键入不再卡顿）；导出 base64 原生编码 + 逐表让出主线程（多表导出期间页面不冻结） |
| 1.9 | 列格式：列设置面板每列新增「格式」下拉（文本 = 默认 / 数字）；数字列导出数值化（'1,234' → 1234，表头不动，解析失败保持原文本），作用于该列及其拆分新列；含合并单元格的表格也可设置；随拆分规则一并持久化 |
| 1.10 | 自适应列宽：导出时逐列按内容最大视觉宽度（中文/全角按 2 字符宽）设置列宽，钳制 6~50 字符；空列取下限、超长列封顶换行，列拆分/筛选/格式应用后的最终列序生效 |
| 1.11 | 多格式导出：工具栏格式下拉切换 xlsx / csv / json / md / html；CSV（RFC4180 + BOM，Excel 直开）、JSON（行对象，数字列数值）、Markdown（GFM 表格）、HTML（完整文档）；多表时 CSV 拆多文件、其余单文件；列设置全格式一致生效；表头判定增强：无 thead 手写表格的 tbody 全 th 行也计为表头（此前 json/md/html 会把表头行当数据） |

## 已知限制（用户须知）

- 虚拟滚动的长列表中，整行内容完全相同且相邻出现的重复行可能少采（分散出现的重复行不受影响）
- 含合并单元格的表格不支持列拆分与列筛选（面板标注禁用，导出保持原样；列格式不受此限）；合并单元格仅在 xlsx 导出还原，csv/json/md/html 为平铺数据
- 拆分规则、列筛选与列格式按「页面 + 表头指纹」记忆：表头变更后不恢复（回落默认需重配）；同页两个表头完全一致的表格共享一份配置；仅存本地不上传
- 分体表格按「表头表 + 数据表视觉拼接」识别：两个独立表格若无缝紧贴且列数一致会被视为一个表格；组件库的汇总行（show-summary 底部合计表）暂不并入导出
- 单元格导出文本与图片链接，不含图片本身和样式；不支持 iframe 内的表格
- 表格需在当前页面已加载（分页数据需先翻页加载）

## 相关文档

- 测试与回归（测试页覆盖矩阵、命令、浏览器回归步骤）：[test/README.md](../test/README.md)
